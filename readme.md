# The Cocktail Hotels

Project with practice purpose

## Dependencies
- Node.js
- npm
- gulpjs

## Installation

To install follow the following steps:
1. Download the code in a local folder
2. Installation of dependencies. Execute the command:
    > npm install
3. Start the local server. Execute the command:
    > gulp

Access via browser to:
http://localhost:8888